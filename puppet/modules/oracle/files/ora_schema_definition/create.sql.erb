insert into schema_version (id, application, version, description, installation_time) values (
  '<%= sequence -%>', 
  '<%= resource[:schema_name] -%>', 
  '<%= resource[:version] -%>',
  '<%= description -%>', sysdate)
